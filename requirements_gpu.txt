# =============================================================================
# AI World Engine V9 - GPU Dependencies (Cloud: L40S / A100 / RTX 4090)
# Install: pip install -r requirements_gpu.txt
# =============================================================================

# Build System (REQUIRED for compiling C++ Kernels & Trellis 2 quickly)
ninja

# Core Deep Learning
torch>=2.1.0
torchvision>=0.16.0

# Diffusion Models (FLUX.1-dev)
diffusers @ git+https://github.com/huggingface/diffusers.git@main
accelerate>=0.28.0
bitsandbytes>=0.43.0
sentencepiece

# Vision Transformers (Depth Anything V3, SAM 3)
transformers @ git+https://github.com/huggingface/transformers.git@main

# SAM 3 (Segment Anything 3)
# To avoid CPU fallback, triton MUST be installed AFTER torch so it inherits the correct binaries.
# See setup_day1.sh for the strict installation order.

# TRELLIS 2
# Installed via: git clone https://github.com/microsoft/TRELLIS.2.git && cd TRELLIS.2 && pip install -e .

# Image Processing
opencv-python>=4.8.0
Pillow>=10.0.0

# 3D Geometry
trimesh>=4.0.0
fast_simplification
xatlas
numpy<2.0.0

# Checkpointing & Serialization
safetensors>=0.4.0

# Super Resolution
realesrgan

# Utils
huggingface_hub>=0.20.0
scipy
